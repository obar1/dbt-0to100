version: 2
sources:
  - name: gf_raw
    schema: |
      {%- if var("source_for_gf_raw") | trim | lower == var("source_for_test_data") -%} {{ target.schema + '_' + var("source_for_test_data") }}
      {%- elif target.name == "qa" -%}  {{ target.schema + '_' + var("source_for_test_data")  }}
      {%- elif target.name == "prod" -%}  {{ var("source_for_gf_raw") }}
      {%- else -%} {{ var("source_for_gf_raw") }}
      {%- endif -%}
    tables:
      - name: apps